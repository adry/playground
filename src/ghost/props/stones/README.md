One module per stone. Each calls registerStone() from ../tombstones.js and
exports nothing; importing the module is what adds it to the set. See the
registry comment in tombstones.js for the contract.
